# 식단·운동 기록 앱 (fitness-log)

개인용 식단/운동 기록 웹앱(PWA). 사용자는 회사 PC, 집 PC, 휴대폰(Claude 앱의 클라우드 세션 / Remote Control)에서 번갈아 작업하며, GitHub 저장소(mkwakm/fitness-log)로 코드를 동기화한다.
클라우드 세션에서 작업했다면 끝날 때 반드시 main에 커밋·push 해서 PC에서 pull 할 수 있게 할 것.

## 구조
- 빌드 도구 없음: 순수 HTML/CSS/JS. `index.html`을 브라우저로 열면 바로 동작.
- `app.js` — 모든 로직. 데이터는 localStorage 키 `fitness-log-v1`에 저장 (구조는 파일 상단 주석 참고).
- `food-db.js` — 음식 표(`FOOD_DB`: 100g당 `kcal100`·`protein` + 단위별 무게, `DEFAULT_UNITS`). 음식을 추가하려면 여기만 고치면 된다.
- `photos.js` — 식단 사진. **IndexedDB**에 따로 저장한다(localStorage는 5MB라 몇 장이면 꽉 찬다). 기록에는 id만 남으므로 **평소 동기화에는 사진이 안 들어간다**. 옮기려면 "사진까지 내보내기"(`collectPhotos` → base64로 한 파일) → 다른 기기에서 가져오기(`restorePhotos`). 안 쓰는 사진은 `prunePhotos()`로 치운다.
- `sync.js` — 기기 간 동기화(GitHub Gist / 파일 자동 저장)와 병합 로직. `app.js`보다 먼저 읽히고, 시작은 `app.js` 끝의 `initSync()`가 한다.
- `sw.js` — 오프라인 캐시. 파일을 수정하면 `CACHE` 버전 문자열을 올릴 것.
- `manifest.json`, `icon.svg` — 홈 화면 설치용.

## 기능 (현재)
- 날짜별 식단: 끼니(아침/점심/저녁/간식), 음식명, 양, 칼로리
- 식단 칼로리 자동 계산: `food-db.js`의 100g당 kcal × 양. 양은 `200g` `1공기` `2개`처럼 숫자+단위로 적고, 단위는 음식별 `units` → `DEFAULT_UNITS` 순으로 찾는다. 음식 이름은 **표의 키로 끝날 때만** 매칭한다(`현미밥`→`밥` ○, `김치전`→`김치` ✗). 칼로리를 직접 고치면 자동값이 덮어쓰지 않는다.
- 날짜별 운동: 종류, 세트별 중량·횟수 기록표(세트 완료 체크, 세트 단위 추가/삭제), 볼륨, 운동 시간(분)
- 집계 기준: **완료 체크한 세트만** 볼륨·소모 칼로리에 들어간다(`countedSets`). 하나도 체크 안 했으면 전체를 집계하므로, 체크를 안 쓰던 기존 기록도 그대로 나온다. 시간을 직접 적었으면 그 값을 쓰고, 안 적었으면 `완료 세트 수 × 3분`으로 추정한다.
- 최고 기록(PR): `personalBest(name)` — 모든 날짜를 훑어 같은 이름 운동의 최고 중량(맨몸이면 최고 횟수)을 찾는다. 동점이면 먼저 세운 날이 남아서, 보고 있는 날이 PR인 경우에만 `🎉 신기록!`을 띄운다.
- 소모 칼로리 자동 계산: `MET × 체중(kg) × 시간(h)`. MET는 운동 이름을 `app.js`의 `MET_TABLE` 정규식과 맞춰 추정하고(기본 5.0), 시간을 안 적으면 세트당 3분으로 추정한다.
- 체중은 **날짜별**(`state.days[날짜].weight`)로 기록한다. `bodyWeight(date)`는 그 날 값 → 그 전 마지막 값 → `profile.weight`(마지막으로 적은 값 캐시) → 70kg 순으로 찾으므로, 과거 기록의 소모 칼로리는 그때 체중으로 계산된다.
- MET 직접 수정: 운동 카드의 MET 칸에서 고치면 `state.profile.mets`(이름 소문자 키)에 저장되고, **같은 이름의 운동에 날짜 상관없이 모두** 적용된다. `↺ 자동` 버튼으로 지우면 다시 `MET_TABLE` 추정값을 쓴다.
- `MET_TABLE`은 위에서부터 먼저 걸리는 줄을 쓰므로 **순서가 중요**하다. 구체적인 이름(`레그프레스`)을 위 줄에, 넓은 키워드(`레그레이즈`가 걸릴 `복근` 줄)를 아래에 둘 것. 기구 이름 전체를 넣지 말고 공통 단어(`프레스`, `로우`, `케이블`)를 잡는다.
- 기록 탭 통계: 기간 토글(7/30/90일)에 따라 요약 타일 + 섭취·소모 막대그래프, 체중 추이, 운동별 중량 추이(그 날의 최고 중량)를 함께 그린다. 기간은 전역 `range`, 선택한 운동은 `liftPick`. 운동·음식·메모 검색, 월별 접기, 날짜별 요약, JSON 내보내기/가져오기
- 부위별 볼륨 추이(`renderVolumeChart`, `partVolumeBuckets`): 고른 부위의 볼륨을 7일이면 하루씩, 30·90일이면 한 주씩 묶어 막대로 그린다. 묶음은 **보고 있는 날에서 거꾸로** 센다(앞에서부터 세면 기간이 딱 안 나뉠 때 오늘이 들어 있는 마지막 며칠이 빠진다). 최근 90일에 볼륨이 0인 부위(맨몸·유산소)는 목록에도 안 넣고, 그런 부위밖에 없으면 카드를 통째로 숨긴다. 보고 있는 부위는 전역 `volPart`.
- 부위별 주간 볼륨 목표(`profile.volGoals`, `volGoal`): 볼륨 그래프 아래 칸에서 부위별로 정한다. **주간 목표라서 견주는 방식이 기간마다 다르다** — 30·90일(막대가 한 주씩)은 목표선을 긋고 "4주 중 2주 달성"을 세고, 7일(막대가 하루씩)은 선을 긋지 않는다(하루 목표가 아니다). 대신 7일 합계가 곧 이번 주 볼륨이라 "%·남은 kg"로 적는다. 달성 주를 셀 때는 `days === 7`인 꽉 찬 묶음만 센다(반쪽 묶음은 미달로 보이기 마련이다). 목표를 정할 때 참고하라고 `avgWeekVolume`(최근 4주 평균, 100kg 단위)을 placeholder와 "평균으로" 버튼에 띄운다. 목표선은 데이터가 아니라 주석이라 `--chart-N`이 아닌 `--muted` 점선을 쓰고, 축 최댓값에 목표도 넣어야(`Math.max(1, goal, …)`) 한참 미달일 때 선이 잘리지 않는다.
- 목표 칸은 `#volGoalBox`로 그래프(`#volChart`)와 **따로** 그린다. 숫자를 치는 동안 그래프만 다시 그리면 입력이 안 끊긴다. 건너뛰는 조건은 **입력칸에 포커스가 있을 때만**이다 — "버튼까지 포함해서" 건너뛰면 "평균으로"를 눌러도 칸에 값이 안 들어간 것처럼 보인다.
- 운동별 메모(`w.note`): 운동 카드의 한 줄 입력칸. 그립·자세·컨디션을 적어두면 같은 운동을 다음에 할 때 "지난번" 줄과 운동 폼 안내(`updateExHint`)에 📝로 따라 붙는다. 기록 탭 검색(`dayText`)에도 들어간다. 비우면 `null`로 되돌아간다.
- 운동 상세(`openExSheet`, `renderExSheet`, `exerciseLog`): 운동 카드의 **이름을 누르면** 그 운동만 모아 보는 전체 화면이 열린다(기록 탭 1RM 카드의 "자세히"로도 열린다). 부위·MET·한 날 수·총 세트·총 볼륨(맨몸이면 총 횟수)·최고 기록·전체 기간 1RM 추이·메모 모음·기록 목록을 담는다. 기록은 `SHEET_ROWS`(30)까지만 그린다(몇 년치를 다 그리면 무겁다).
  - 시트는 **`main` 밖**에 둔다. 안에 두면 시트 안에서 좌우로 쓸 때 날짜가 넘어간다.
  - 열 때 `history.pushState`로 한 칸 넣어서 **휴대폰 뒤로가기로 닫히게** 한다(안 넣으면 뒤로가기에 앱이 꺼진다). ✕·ESC로 닫을 땐 `history.back()`으로 그 칸을 도로 뺀다 — 안 빼면 닫은 뒤 뒤로가기가 먹통이 된다.
  - 열려 있는 동안 `render()`가 `renderExSheet()`도 부른다. 안 부르면 방금 적은 세트가 상세에 안 보인다.
- 운동 순서는 카드의 ↑↓ 버튼으로 바꾼다. 가로 스와이프로 날짜를 넘기고 있어서 드래그 제스처와 충돌하므로 드래그는 쓰지 말 것.
- 식단 사진(`photos.js`): 고르면 긴 변 900px·JPEG로 줄여 담는다(원본은 한 장에 5MB도 넘는다). 그린 뒤 `fillPhotos()`가 채우고, 만든 blob URL은 다시 그릴 때 `releasePhotoUrls()`로 반드시 정리한다(안 하면 메모리가 샌다). 식단을 지워도 사진은 바로 안 지우고 되돌리기 시간이 지난 뒤 지운다(`photosToDrop`).
- 주간 리포트(`weekReport`, 기록 탭 맨 위): 최근 7일을 그 앞 7일과 견줘 운동한 날·세트·볼륨·시간·소모를 %로 보여주고, 그 주에 세운 신기록을 모은다. 평균 섭취는 **기록한 날 수로만** 나눈다(안 적은 날까지 나누면 평균이 엉뚱해진다).
- 운동 자동완성: 내가 쓴 이름이 앞, `COMMON_EX`(흔한 운동 54종)가 뒤. `metKey` 기준으로 중복을 걸러서 "벤치프레스"와 " 벤치프레스 "가 따로 뜨지 않는다. 표기가 흔들리면 최고기록·추이가 갈리므로 되도록 목록에서 고르게 유도한다.
- 세트 사이 휴식: 세트를 완료할 때 `set.doneAt`(시각)을 남기고, 그 간격으로 평균 휴식을 낸다(`restGaps`, `avgRest`). 20분이 넘는 간격은 자리를 비운 것으로 보고 뺀다.
- 부위별 주간 목표(`profile.partGoals`): 정해두면 밸런스가 `3/12` 형태로 바뀌고 남은 세트를 알려준다. **최근 7일을 볼 때만** 견준다(주간 목표라서).
- 점진적 과부하 제안(`overloadTip`): 지난번에 **완료 체크한 세트가 2개 이상**이고 그 세트들의 횟수가 모두 첫 세트 이상이면 "다 채웠다"로 보고 다음 중량(± 단위만큼)을 제안한다. 맨몸 운동은 횟수를 +2. 운동 폼과 운동 카드에 함께 띄우고, 폼의 중량칸이 비어 있을 때만 미리 채운다(직접 적은 값은 건드리지 않는다).
- 오늘의 계획(`day.plan`): 루틴 이름만 그 날짜에 적어두고, 남은 것은 그때그때 기록과 견줘 계산한다(`planItems`). 계획을 복사해 두면 나중에 루틴을 고쳐도 옛날 계획이 남으므로 **이름만 저장할 것**. 루틴을 통째로 불러오면 그게 곧 그 날의 계획이 된다.
- 루틴(`profile.routines`): 오늘 운동을 이름 붙여 저장하고 한 번에 불러온다. **중량은 저장하지 않는다** — 불러올 때 그 운동의 최근 기록에서 가져온다(석 달 전 무게가 따라오면 안 되므로). 이름은 그날 많이 한 부위로 제안한다(`suggestRoutineName`).
- 입력 편의: 지난 기록 표시(`lastRecord`), 지난 운동/식단 통째로 불러오기, 세트별 중량·횟수 ± 버튼, 세트 완료 시 휴식 타이머(WebAudio 알림음), 날짜별 메모
- 단백질: 음식별 100g당 단백질로 하루 합계를 내고 달성 막대를 보여준다. 목표를 안 적었으면 `체중 × 1.6g`.
- 1RM(`oneRM`, Epley): 최고 중량만 보면 `100kg×1`이 `90kg×8`보다 센 기록으로 잡혀서, 세트 비교와 `personalBest`·중량 추이 그래프는 모두 1RM을 쓴다. 1회는 그 무게가 곧 1RM이고(공식대로면 3% 부풀려짐) 12회를 넘으면 12회로 제한한다.
- 부위 분류(`PART_TABLE`, `partOf`): 가슴/등/하체/어깨/팔/코어/유산소/전신/기타. `MET_TABLE`과 같은 "먼저 걸리는 줄" 규칙이라 순서가 중요하다. **팔을 가슴·어깨보다 먼저** 봐야 "케이블푸시다운"이 케이블(가슴)로 새지 않는다. 흔한 운동 이름 63개로 검증했으니 고칠 땐 다시 돌려볼 것.
- 부위별 밸런스는 **계획용**이다. 안 한 부위도 `MAIN_PARTS`로 같이 보여주고, 오래 쉰 순으로 세우고, 각 부위를 마지막으로 한 날(`lastPartDay`)을 "5일 전 / 어제 / 안 함"으로 적고, 2일 이상 쉰 주요 부위 둘을 "오늘은 ○○ 차례"로 제안한다. 세트 수만 보여주면 오늘 뭘 할지 알 수 없다.
- 꾸준함: `workoutStreak()` 연속 운동 일수(오늘 아직 안 했어도 어제까지 이어온 기록은 살아 있다), `weeklyCount()` 이번 주(월요일 시작) 운동한 날, 목표는 `profile.weeklyGoal`.
- 운동 전체 시간: 운동 탭의 시작/끝내기 버튼. 진행 중이면 `day.sessionStart`(시각), 끝내면 `day.sessionMin`에 분으로 누적된다.
- 화면 꺼짐 방지(`keepScreenOn`): 세션이 도는 동안만 Wake Lock을 잡는다. 화면을 가리면 브라우저가 알아서 풀어버리므로 `visibilitychange`에서 다시 잡아야 한다. 거절당해도(배터리 절약 모드 등) 조용히 넘어가고 기록은 그대로 동작해야 한다.
- 물 섭취: `day.water`(잔), 목표는 `profile.waterGoal`(기본 8).
- 식단 편의: 자주 먹는 음식 원터치 추가, 표에 없는 음식 직접 등록(`profile.foods`, 기본 표보다 우선), 하루 목표 칼로리(`목표 + 소모 − 섭취 = 남은 칼로리`)
- 안전장치: 삭제·가져오기 전에 `pushUndo()`로 전체 상태를 스냅샷 → 스낵바에서 7초 안에 되돌리기
- 날짜 이동: ‹ › 버튼, 좌우 스와이프, 오늘이 아닐 때만 뜨는 "오늘" 버튼. 어느 쪽이든 `goToDate(날짜, 방향)`을 거쳐야 연출이 붙는다. `currentDate`를 직접 바꾸고 `render()`하면 조용히 바뀌어 티가 안 난다.
- 날짜 넘김 연출은 **`main` 하나만** 움직인다(나가기 120ms → 내용 교체 → 들어오기 300ms). 나가는 요소와 들어오는 요소를 따로 두면 방향이 엇갈려 서로 상쇄되고, 내용 길이에 따라 어떤 부분만 움직이는 것처럼 보인다.
- 연출 중에도 `currentDate`는 **먼저 확정**한다(안 그러면 빠르게 두 번 눌러도 하루만 넘어간다). 대신 그 120ms 동안은 화면과 날짜가 어긋나므로 `main.leaving { pointer-events: none }`으로 못 누르게 막고, `findWorkout()` 결과는 항상 없을 수 있다고 보고 다룬다.
- 기기 간 동기화(`sync.js`): ① PC는 File System Access API로 클라우드 드라이브 파일에 자동 저장 ② 휴대폰은 내보내 공유(`navigator.share`) → 가져오기 ③ GitHub Gist(토큰 필요, 접어둠). 셋 다 같은 `mergeState()`를 쓴다.

## 동기화 규칙 (sync.js)
- **토큰 없는 방법이 우선이다.** 화면에서 파일 방식과 휴대폰 방식을 위에 두고, 토큰이 필요한 Gist는 `<details class="sync-adv">`로 접어 둔다. 기본값을 토큰 쪽으로 되돌리지 말 것.
- 접힘 상태는 `advOpen`에 기억한다. 다시 그릴 때 이 값을 안 쓰면 사용자가 펼쳐 둔 게 도로 접힌다.
- **토큰은 절대 `state`에 넣지 말 것.** 별도 키 `fitness-log-sync`에 둔다. 백업 JSON 내보내기에 섞이면 안 된다.
- 병합은 **항목 단위**다. 날짜를 통째로 덮으면 "집 PC에서 점심, 휴대폰에서 저녁"을 적었을 때 한쪽이 사라진다. 식단·운동은 `id`로 합치고, 지운 항목은 `day.deleted`에 id를 남겨 삭제가 다른 기기에도 전달되게 한다(`markDeleted`). 메모·체중처럼 값이 하나뿐인 건 `updatedAt`이 최근인 쪽을 쓴다.
- `updatedAt`은 `save()` 안의 `stampChanges()`가 **바뀐 날짜에만** 자동으로 찍는다. 수정 지점마다 손으로 찍지 말 것.
- 백업 **가져오기도 `mergeState()`로 합친다**. 덮어쓰면 휴대폰↔PC를 파일로 주고받을 때 기록이 사라진다.
- 파일을 새로 지정할 때는 **읽고 나서 쓴다**. 다른 기기에서 쓰던 파일을 고른 경우 덮어쓰면 그 기록이 날아간다.
- IndexedDB 콜백 안에서 난 예외는 밖으로 새어나가 promise가 영영 안 끝난다. `idb()`처럼 꼭 try/catch로 감쌀 것.
- `mergeDay`에서 **빈 `deleted`는 넣지 말 것.** 내용이 같은 날도 "바뀜"으로 보여서 동기화할 때마다 모든 날의 `updatedAt`이 밀리고, 상대 기기가 나중에 쓴 메모·체중을 덮어쓴다.
- 밖에서 들어온 날짜 데이터는 `safeDay()`를 통과시킬 것. 손상된 값이 저장되면 앱이 아예 안 열린다(`load()`의 `sanitizeDays`도 같은 이유).
- 자동 동기화: 열어둔 동안 5분마다(`AUTO_SYNC_MS`), 기록을 고치고 45초 잠잠하면(`syncAfterEdit`), 앱으로 돌아왔는데 마지막 동기화가 2분 넘었으면 돈다. 전부 `gistSync({ quiet: true })` — **바뀐 게 없으면 아무 말도 안 하고, 입력 중(`activeElement`가 입력칸)이면 건너뛴다**(화면을 갈아엎으면 입력이 끊긴다). 겹쳐 돌지 않게 `syncing` 플래그로 막는다.
- 파일에 못 쓰게 됐을 때 조용히 넘어가지 말 것(`fileBlocked`). 브라우저를 다시 열면 권한이 풀리는데, 안 알리면 저장된 줄 알고 기록을 잃는다.

## 휴대폰 (주 사용 환경)
- **입력칸 글자는 16px 밑으로 내리지 말 것.** iOS 사파리는 16px 미만 입력칸을 누르면 화면을 확대해 버린다. 라벨은 작게 두되 `input, select, textarea { font-size: 16px }`를 이기는 규칙을 만들지 말 것.
- 자주 누르는 버튼은 **높이 40px 이상**. 세트 행의 ±·완료·삭제가 여기에 해당한다.
- 아래 고정 바(휴식 타이머·되돌리기)는 `.dock` 안에 넣어 세로로 쌓는다. 따로 `position: fixed`로 두면 같이 떴을 때 겹친다.
- 긴 안내 문구는 `<details class="help">`로 접는다. 좁은 화면에서 글 벽이 된다.
- 375px(아이폰 SE) 기준으로 가로 스크롤이 생기지 않는지 확인할 것. 스와이프 중 `main`이 움직이므로 `main { overflow-x: clip }`을 지우지 말 것.
- 스와이프는 가로로 16px 넘게 움직였고 세로보다 클 때만 따라간다(`touch.engaged`). 이 판정을 느슨하게 하면 세로 스크롤 중에 날짜가 넘어간다.
- 애니메이션은 `prefers-reduced-motion`을 존중할 것(`reduceMotion()`).

## 조심할 것 (실제로 났던 문제)
- **기록 탭의 접힌 달은 속을 비워 둔다**(`monthDaysHtml`). 3년치면 1100장이라 접힌 달까지 다 그리면 안 보이는 카드로 DOM이 무거워진다. `toggle`에서 `children.length === 1`(summary만 있음)일 때 채운다.
- **화면에서 막아도 데이터는 들어온다.** `<input type=number min=0>`은 폼을 제출할 때만 검사하므로 세트 칸에 `-50`을 쳐 넣을 수 있고, 망가진 동기화 파일로도 음수가 들어온다. 집계(`volumeOf`·`dayIntake`·`dayProtein`)는 `nonNeg()`로 음수를 0으로 본다.
- **입력 중에는 `save()` 대신 `saveSoon()`**. 글자마다 전체를 직렬화하면 기록이 쌓일수록 느려진다. 화면은 즉시 갱신하고 저장만 미루며, `visibilitychange`·`pagehide`에서 `flushSave()`로 반드시 반영한다.
- **휴식 타이머는 벽시계(`restEndAt`) 기준.** 1초씩 빼면 화면이 꺼졌을 때 브라우저가 타이머를 늦춰 실제보다 느려진다.
- **`<input type=file>`은 값을 먼저 비운다.** 끝에서 비우면 중간에 취소로 빠져나갈 때 안 비워져서 같은 파일을 다시 못 고른다.
- **사용자 입력이 키가 되는 집계는 `Map`을 쓸 것.** 일반 객체면 "constructor" 같은 이름이 프로토타입을 건드린다. 객체에서 값을 꺼낼 때도 `Object.hasOwn`으로 확인할 것(안 하면 "1toString" 같은 입력이 NaN이 된다).
- **다시 그려도 유지돼야 하는 펼침 상태는 따로 기억할 것** (`monthOpen`, `advOpen`). `render()`가 HTML을 통째로 갈아끼우기 때문에 그냥 두면 도로 접힌다.

## 작업 규칙
- 답변과 UI 문구는 한국어.
- 그래프 색은 `--chart-1`(섭취) `--chart-2`(소모) `--chart-3`(체중) `--chart-4`(운동 중량) `--chart-5`(부위별 볼륨) 변수만 쓴다. dataviz 기준으로 5색 함께 검증한 값이므로(다크·라이트 모두 통과) 바꾸려면 `validate_palette.js`로 다시 검증할 것. 한 그래프에 축은 하나만 두고, 두 번째 y축을 만들지 말 것 (단위가 다르면 그래프를 나눈다).
- 화면은 어두운 톤이 기본. 색은 `style.css`의 `:root` 변수로만 쓰고, 밝은 테마 값은 `:root[data-theme="light"]`에 둘 것. 테마는 `state.profile.theme`에 저장.
- 작업 시작 전 `git pull`, 작업 후 커밋하고 `git push` 해서 다른 PC와 동기화.

## 배포
- GitHub Pages로 서비스한다: https://mkwakm.github.io/fitness-log/
- **`file://`로 열면 서비스워커도 홈 화면 설치도 동작하지 않는다.** 휴대폰에서 쓰려면 반드시 https 주소로 접속할 것.
- 아이콘을 바꾸면 SVG만 말고 `icon-192/512/maskable-512.png`도 같이 갱신할 것 (PNG가 없으면 안드로이드 설치 배너가 안 뜬다).

## 아이디어 (다음 할 일 후보)
- 식단 사진에서 음식 이름 추측 — 빌드 도구도 서버도 없는 앱이라 **모델을 태울 자리가 없다.** 외부 API를 부르면 사진이 기기 밖으로 나가고 토큰도 필요해서, 지금 구조에서는 넣지 않는 게 맞다.
