# 💪 나의 식단·운동 기록

오늘 먹은 음식과 운동(종류, 세트, 횟수, 무게, 시간)을 기록하는 개인용 웹앱.

## 🔥 소모 칼로리 자동 계산
운동 탭에 체중을 한 번 적어두면, 운동을 추가할 때마다 소모 칼로리가 자동으로 계산됩니다.

- 계산식: `MET × 체중(kg) × 운동 시간(h)`
- MET(운동 강도)는 운동 이름으로 자동 추정합니다. (러닝 8.0, 스쿼트 6.0, 벤치프레스 5.0, 걷기 3.5 …)
- 운동 시간을 안 적으면 **세트당 3분**으로 추정하고 `(추정)` 이라고 표시합니다.
- 식단·기록 탭에서는 `섭취 − 소모 = 순 칼로리`도 같이 보여줍니다.

어디까지나 추정치라서 실제 소모량과는 차이가 있을 수 있습니다.

## 실행
`index.html`을 브라우저에서 열면 됩니다. 설치할 것 없음.

## 다른 PC에서 이어서 작업하기
```bash
git clone https://github.com/mkwakm/fitness-log.git
```
- 작업 시작 전: `git pull`
- 작업 끝난 후: `git add -A && git commit -m "메시지" && git push`

📱 휴대폰 연결 테스트 완료

☁️ 클라우드 세션 테스트 완료
